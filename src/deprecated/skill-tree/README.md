# Deprecated skill tree

The skill tree is disabled. We may revisit it later, so its implementation and UI assets stay together in this directory for reference.

Production code must not import, link, or depend on files in this directory.

To re-enable it, first move the implementation back into active source directories. Then restore the archived markup and styles, reconnect point awards and input/debug entry points, and add coverage for the restored gameplay effects before linking it from production.
